/*jslint browser: true*/
///*globals io */
(function() {
	'use strict';

	// angular
	// .module('policynet')
	// .run(createSocket);

	/** @ngInject */
	// function createSocket(toastr){
	// 	var socketURL = window.location.protocol + "//" + window.location.hostname + ":" + window.location.port;
	// 	var socket = io.connect(socketURL);
	// 	socket.on('message', function (data) {
	// 		toastr.info(data, "", {timeOut: 10000});
	// 	});
	// }
})();
